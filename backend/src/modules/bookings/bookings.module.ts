import { Module } from '@nestjs/common';

/**
 * BookingsModule
 * Manages transactional booking creation, conflicts prevention, booking lifecycle,
 * and the BookingProvider abstraction.
 * Implemented in Phase 2 Increments 6 & 7.
 */
@Module({})
export class BookingsModule {}
