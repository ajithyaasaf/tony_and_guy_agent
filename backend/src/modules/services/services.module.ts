import { Module } from '@nestjs/common';

/**
 * ServicesModule
 * Manages service categories, service rituals, pricing, and duration rules.
 * Implemented in Phase 2 Increment 4.
 */
@Module({})
export class ServicesModule {}
