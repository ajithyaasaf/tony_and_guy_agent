import { Module } from '@nestjs/common';

/**
 * AvailabilityModule
 * Manages live salon capacity, multi-service slot calculation, and provider boundaries.
 * Implemented in Phase 2 Increment 5.
 */
@Module({})
export class AvailabilityModule {}
