import { Module } from '@nestjs/common';

/**
 * OutletsModule
 * Manages salon locations, working hours, and salon discovery.
 * Persistence and full CRUD services are implemented in Phase 2 Increment 4.
 */
@Module({})
export class OutletsModule {}
